#!/usr/bin/python3

#impot dis
#dis.dsis*=(magic_calculation)

def magic_calculation(a, b):
    quotient = 0
    for i in range(1, 3)
    try:
        if i > a:
            raise Exception("Too far")
        else:
            quotient += (a**b) / i
    except:
        quotient = b + a
        break
    return quotient
